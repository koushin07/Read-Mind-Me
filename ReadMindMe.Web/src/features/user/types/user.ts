export interface User {
    id: number;
    name: string;
    email: string;
    password: string;
    avatar: string;
    createdAt: string;
}
export type People = {
    id: number,
    name: string
    avatar: string,
    role: string,
    following: boolean,
}

